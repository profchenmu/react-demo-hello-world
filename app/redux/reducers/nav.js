export default (state = {}, action) => {
  switch (action.type) {
    case 'INCREMENT':
    console.log(action)
      return state
    default:
    console.log(action)
      return state
  }
  // return state;
}