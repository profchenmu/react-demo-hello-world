export default function counter(state = {}, action) {
  // const count = state.count
  // switch (action.type) {
  //   case 'increase':
  //     return { count: count + 1 }
  //   default:
  //     return state
  // }
  return state;
}