export default (state = {}, action) => {
  switch (action.type) {
    case 'LOGIN':
    console.log(action)
      return state
    default:
    console.log(action)
      return state
  }
  // return state;
}